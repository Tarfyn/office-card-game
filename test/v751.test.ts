import { strict as assert } from "node:assert";import { readFileSync } from "node:fs";import { fileURLToPath } from "node:url";let passed=0;const root=(n:string)=>readFileSync(fileURLToPath(new URL(`../../${n}`,import.meta.url)),"utf8");const app=root("public/app.js"),css=root("public/styles.css"),server=root("server/server.mjs"),html=root("public/index.html"),pkg=JSON.parse(root("package.json"));function test(n:string,f:()=>void){f();passed++;console.log(`✓ ${n}`)}test("v7.51 version markers",()=>{assert.ok(Number(String(pkg.version).split(".")[1])>=51);assert.match(server,/version: "7\.\d+\.0"/);assert.match(html,/v7\.\d+ Alpha Playtest/i)});test("first run guide explains core resources",()=>{assert.match(app,/Company Reputation/);assert.match(app,/Capacity/);assert.match(app,/Main → Battle → End/)});test("guide is skippable and reopenable",()=>{assert.match(app,/ALPHA_ONBOARDING_KEY/);assert.match(app,/openAlphaGuide/);assert.match(app,/skipAlphaOnboarding/)});test("guide hands off to Office Coach",()=>assert.match(app,/Office Coach highlights legal actions/));test("onboarding is responsive",()=>{assert.match(css,/alpha-onboarding-grid/);assert.match(css,/@media\(max-width:430px\)/)});test("no gameplay rule mutation is introduced",()=>assert.doesNotMatch(app,/sendIntent\(\{type:'TUTORIAL/));console.log(`${passed}/6 v7.51 tests passed.`);