import { strict as assert } from "node:assert";import { readFileSync } from "node:fs";import { fileURLToPath } from "node:url";let passed=0;const root=(n:string)=>readFileSync(fileURLToPath(new URL(`../../${n}`,import.meta.url)),"utf8");const server=root("server/server.mjs"),pkg=JSON.parse(root("package.json")),doc=root("ALPHA_CANDIDATE_v7.56.md"),check=root("scripts/alpha-candidate-check.mjs");function test(n:string,f:()=>void){f();passed++;console.log(`✓ ${n}`)}test("v7.56 candidate contract remains documented",()=>{assert.match(doc,/External Alpha Candidate/i);assert.match(server,/releaseChannel:"EXTERNAL_ALPHA_CANDIDATE"/)});test("candidate ships a release check",()=>{assert.equal(pkg.scripts["ops:alpha-check"],"node scripts/alpha-candidate-check.mjs");assert.match(check,/ALPHA_CANDIDATE_OK/)});test("candidate check freezes 107 cards and five 40-card starters",()=>{assert.match(check,/defs\.length!==107/);assert.match(check,/presets\.length!==5/);assert.match(check,/cardCount\(deck\)!==40/)});test("candidate check requires hosted-alpha deployment assets",()=>{assert.match(check,/Caddyfile\.example/);assert.match(check,/EXTERNAL_ALPHA\.md/);assert.match(check,/office-card-game\.service\.example/)});test("candidate document records known operational limits",()=>{assert.match(doc,/local JSON/);assert.match(doc,/timer remains OFF/i);assert.match(doc,/real hosted two-network smoke/i)});test("candidate retains tester-readiness tools",()=>{for(const m of ["renderAlphaOnboarding","renderConnectionDiagnosticsPanel","buildBugReportBundle","alphaTestSessionId","scheduleStreamReconnect"])assert.match(check,new RegExp(m))});console.log(`${passed}/6 v7.56 tests passed.`);