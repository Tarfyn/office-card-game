import { strict as assert } from "node:assert";import { readFileSync } from "node:fs";import { fileURLToPath } from "node:url";let passed=0;const root=(n:string)=>readFileSync(fileURLToPath(new URL(`../../${n}`,import.meta.url)),"utf8");const pkg=JSON.parse(root("package.json")),server=root("server/server.mjs"),html=root("public/index.html"),app=root("public/app.js"),css=root("public/styles.css"),de=root("public/locales/de.js");function test(n:string,f:()=>void){f();passed++;console.log(`✓ ${n}`)}
test("v7.65 tester UX cleanup remains present after later versions",()=>{assert.match(app,/function renderLobbyConnectionStatus\(\)/);assert.match(app,/MATCH HISTORY & SUPPORT/);assert.match(css,/external Alpha UX cleanup/)});
test("normal lobby collapses technical diagnostics into support tools",()=>{assert.match(app,/function renderLobbyConnectionStatus\(\)/);assert.match(app,/MATCH HISTORY & SUPPORT/);assert.match(app,/renderConnectionDiagnosticsPanel\(\)/);assert.doesNotMatch(app,/renderConnectionDiagnosticsPanel\(\{ compact:true \}\)/)});
test("ordinary testers do not request admin analytics on boot",()=>{assert.match(app,/if \(state\.adminToken && opsModeAvailable\(\)\) await refreshPlaytestAnalytics\(false\)/);assert.match(app,/adminAnalytics=state\.adminToken && opsModeAvailable\(\)/)});
test("profile chrome hides storage and auth jargon in the normal path",()=>{assert.match(app,/Guest profile · progress saved on this server/);assert.doesNotMatch(app,/\$\{esc\(storage\)\} · \$\{esc\(state\.serverAccount\?\.authMode/)});
test("collection chrome removes sandbox jargon without changing collection modes",()=>{assert.match(app,/Build, collect and inspect the full office set/);assert.match(app,/All Alpha cards available/);assert.match(app,/data-collection-mode="SANDBOX_ALL_AVAILABLE"/)});
test("new tester-facing cleanup strings have German overlays",()=>{assert.match(de,/MATCHVERLAUF & HILFE/);assert.match(de,/Gastprofil/);assert.match(css,/v7\.65 — external Alpha UX cleanup/)});
console.log(`${passed}/6 v7.65 tests passed.`);
