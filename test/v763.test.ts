import { strict as assert } from "node:assert";import { readFileSync } from "node:fs";import { fileURLToPath } from "node:url";let passed=0;const root=(n:string)=>readFileSync(fileURLToPath(new URL(`../../${n}`,import.meta.url)),"utf8");const pkg=JSON.parse(root("package.json")),server=root("server/server.mjs"),html=root("public/index.html"),app=root("public/app.js"),css=root("public/styles.css"),art=JSON.parse(root("data/artwork.json")),audit=root("scripts/artwork-audit.mjs"),build=root("scripts/build.mjs");function test(n:string,f:()=>void){f();passed++;console.log(`✓ ${n}`)}
test("v7.63 artwork focus system remains present after later versions",()=>{assert.match(server,/artFocus: artworkConfig\.focusByCard/);assert.match(app,/function artworkFocusStyle\(def\)/);assert.match(css,/per-card artwork focus/)});
test("artwork focus metadata is presentation-only and separately stored",()=>{assert.equal(art.version,1);assert.ok(art.focusByCard['CS-001']);assert.match(build,/artwork\.json/);assert.match(server,/artFocus: artworkConfig\.focusByCard/)});
test("focus values are reused by live and modal card artwork",()=>{assert.match(app,/function artworkFocusStyle\(def\)/);assert.match(app,/className} has-art" style="\$\{artworkFocusStyle\(def\)\}/)});
test("catalog and collection artwork use the same focus metadata",()=>{assert.match(app,/style="\$\{artworkFocusStyle\(def\)\}"/);assert.match(css,/per-card artwork focus/);assert.match(css,/object-position:var\(--art-focus-x,50%\) var\(--art-focus-y,50%\)/)});
test("focus metadata stays bounded and auditable",()=>{assert.match(audit,/focus must be x\/y 0-100/);assert.match(audit,/unknown card id/);assert.match(audit,/focusProblems/)});
test("all seeded focus points are valid percentages",()=>{for(const focus of Object.values<any>(art.focusByCard)){assert.ok(focus.x>=0&&focus.x<=100);assert.ok(focus.y>=0&&focus.y<=100)}});
console.log(`${passed}/6 v7.63 tests passed.`);
