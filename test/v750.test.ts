import { strict as assert } from "node:assert";import { readFileSync } from "node:fs";import { fileURLToPath } from "node:url";let passed=0;function test(n:string,f:()=>void){f();passed++;console.log(`✓ ${n}`)}const root=(n:string)=>readFileSync(fileURLToPath(new URL(`../../${n}`,import.meta.url)),"utf8");const server=root("server/server.mjs"),app=root("public/app.js"),css=root("public/styles.css"),guide=root("deploy/EXTERNAL_ALPHA.md"),smoke=root("scripts/external-alpha-smoke.mjs");const pkg=JSON.parse(root("package.json"));
test("v7.50 external-alpha baseline remains on later versions",()=>{assert.ok(Number(String(pkg.version).split(".")[1])>=50);assert.match(server,/version: "7\.\d+\.0"/);assert.match(server,/Office Card Game v7\.\d+ server/);});
test("v7.50 creates credential-free room invite links",()=>{assert.match(app,/function inviteUrl\(roomId\)/);assert.match(app,/\?join=/);assert.match(app,/Copy invite link/);assert.match(guide,/never a seat\/profile credential/);});
test("v7.50 pre-fills join code from an invite URL",()=>{assert.match(app,/new URLSearchParams\(location\.search\)\.get\('join'\)/);assert.match(app,/state\.inviteRoomCode = invited\.toUpperCase/);assert.match(app,/value="\$\{esc\(state\.inviteRoomCode/);});
test("v7.50 surfaces compact network diagnostics",()=>{assert.match(app,/renderNetworkDiagnostic/);assert.match(css,/network-diagnostic/);assert.match(app,/state\.serverInfo = health/);});
test("v7.50 ships an executable two-seat external alpha smoke",()=>{assert.equal(pkg.scripts["ops:external-smoke"],"node scripts/external-alpha-smoke.mjs");assert.match(smoke,/stream-ticket/);assert.match(smoke,/EXTERNAL_ALPHA_OK/);assert.match(guide,/players do not need to open ports on their own networks/);});
console.log(`${passed}/5 v7.50 tests passed.`);
